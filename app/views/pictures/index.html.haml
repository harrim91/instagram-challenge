- if @pictures.any?
  .pictures-wrapper.row
    -@pictures.each do |picture|
      .picture
        .image.center-block= link_to(image_tag(picture.image.url(:medium), class: 'img-responsive', alt: picture.description), picture_path(picture))
        %p.description= picture.description